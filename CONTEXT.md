# 石头鱼的工程规则

本文件统一维护工作流的领域术语，供维护者、文档和评测使用，不由 Hook 注入。运行时唯一权威是独立安装的核心 [SKILL.md](skills/stonefish-engineering/SKILL.md) 及其 references，Hook 插件只持有加载要求。

## 核心术语

**常驻工程执行契约**：
每个工程任务从理解、设计、实施到验证都共同应用的一组方法与优先级。它们参与方案生成，不是交付前检查，也不要求每次制造对应结构。
_避免_：常驻工程检查、最佳实践签到、事后合规扫描

**常驻方法**：
在绝大多数工程任务中持续改变目标、所有权、职责边界、复杂度、修改范围或证据要求的方法。全部常驻方法共同生效，但公开回复不机械列出全部名称。
_避免_：默认架构模板、每次都实例化的设计模式

**条件方法**：
只有项目事实出现明确触发信号时才采用的具体架构形态、迁移手法或验证方法；必须同时记录反触发和退出条件。
_避免_：可选建议、想起来才用的方法、方法论书单

**加载要求送达**：
受信任的 Hook 在规定生命周期把明确的 Skill 加载要求放入模型上下文。送达只表示要求可见，不等于规则正文已加载。
_避免_：规则已生效、正文已读取、行为保证

**规则正文加载**：
独立 Skill 的完整核心正文已进入 Agent 当前有效上下文，成为工程决定的规则依据。它与按需读取的 references 共同构成运行时规则内容，不等于规则已落实。
_避免_：只收到 Hook 要求、只读 Skill 元数据、宿主调用计数增加

**规则落实**：
模型在最终方案、代码、迁移、风险处理和验证中表现出契约要求的可观察行为。通过固定真实任务评测提高可靠性，不能宣称绝对确定。
_避免_：Hook 已运行、正文已读取、方法名已出现

**独立 Skill 与 Hook 插件**：
Skill 持有唯一规则正文及 references，Hook 插件只持有生命周期加载要求。二者是职责与更新边界独立的组件，插件不携带第二份 Skill。
_避免_：更新 Skill 等于更新插件、旧新插件并存、同名 Skill 重复注册

**知识级 DRY**：
同一业务知识只有一个权威来源，修改该知识不需要同步维护多个副本。两段语法相似但变化原因独立的代码不构成知识重复。
_避免_：零重复代码、按行数抽象、相似代码必须合并

**长期单轨**：
目标状态只保留一个权威实现路径；临时兼容或迁移双轨必须有真实消费者、负责人、退出条件和删除证据。
_避免_：永远兼容、无期限 feature flag、新旧实现长期并存

## 用户可见术语

**公开决策说明**：
解释石头鱼的工程规则如何根据当前事实形成可观察工程决定，以及该决定的收益、代价和剩余风险；不是隐藏思维过程或行为证明。
_避免_：工程依据、思维链、规则检查报告

**方法论**：
工程原则、架构方法、改造策略和验证方法的总称。展示具体条目时使用准确名称与分类。
_避免_：最佳实践清单、规则库存

**方法论采用**：
某项方法实质改变了所有者、范围、Interface、依赖、风险、兼容或验证。只读取、检查、与结果相容或事后贴名不构成采用。
_避免_：方法论符合、规则已读取

**独立决定**：
可以单独改变方案、拥有自己的事实依据和影响的工程选择。公开说明以决定组织，不以方法名组织。
_避免_：方法论条目、执行步骤

**影响与取舍**：
当前决定实际带来的收益、代价、剩余风险和改变推荐的条件。方案阶段只能写预期，交付阶段才写已形成影响和验证证据。
_避免_：脱离场景的通用优缺点、未验证收益
