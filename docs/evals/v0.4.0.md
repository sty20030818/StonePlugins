# v0.4.0 候选评测

状态：历史候选证据，原位归档。本文的 HEAD、未发布状态、检查与未完成边界仅对应记录时点，不证明当前安装状态或新重构通过。现行文档见[文档索引](../README.md)；后续目标与未完成验收见 [ADR-0004](../adr/0004-codex-first-bundled-engineering.md) 和[重构计划](../plans/2026-09-14-codex-first-refactor.md)。

本文件记录 `stonefish-engineering` v0.4.0 的本地候选证据。它验证常驻工程执行契约、条件方法路由和公开决策说明，不等同于已发布版本或真实客户端安装验收。

## 环境与边界

- 日期：2026-08-31（Asia/Shanghai）
- 发布基线：`v0.3.7`（`5e76755`）
- 候选：桌面 `StonePlugins` 仓库的未提交 `v0.4.0` 工作树
- 当前 HEAD：`59ffbb9`；`git ls-remote` 确认远端 `main` 已指向同一提交，本地 `origin/main` 跟踪引用仍停在 `5e76755`，因此本地状态显示 ahead 1
- `0.4.0` 候选内容尚未提交、推送、打 tag 或创建 GitHub Release；最新公开稳定版仍为 `v0.3.7`
- 候选来源：核心 `SKILL.md`、条件 references、`docs/methodologies.md`、ADR-0002、研究记录及固定行为案例
- 本地确定性环境：Node.js `24.16.0`、npm `11.13.0`；Skill 校验使用隔离环境中的 PyYAML `6.0.2`
- 证据边界：确定性检查只能证明规则送达和仓库契约；模型是否落实规则仍需行为评测，不能承诺所有模型在所有对话中绝对遵守

## 候选目标

1. 所有常驻方法从理解开始共同形成工程决定，不再退化为交付前检查。
2. 整洁架构与六边形架构的领域独立、依赖方向和边界原则常驻，具体结构仍按真实信号实例化。
3. 条件方法由聚焦选择器路由；完整方法目录和治理信息不进入每轮上下文。
4. 公开决策说明展示“事实与约束 → 方法作用 → 决定 → 影响与取舍”，但不承担规则落实证明。
5. 全局 `AGENTS.md` 继续只承载日常协作和个人偏好，避免与插件形成第二份工程事实源。

## 确定性检查

| 检查 | 结果 | 证据边界 |
| --- | --- | --- |
| `npm run check` | PASS | 类型检查、构建、仓库契约和 5 个 Hook 测试全部通过 |
| 发布态 Git 跟踪门禁 | PASS（隔离候选） | `npm run validate:release` 在隔离 Git 副本中将全部候选文件加入索引后通过；真实工作树因新文件尚未跟踪而按预期拒绝通过，提交前不得忽略 |
| Skill 与插件专用校验器 | PASS | 隔离 PyYAML 环境中的 `quick_validate.py` 和 `validate_plugin.py` 均通过；PyYAML 不属于插件运行依赖 |
| `git diff --check` | PASS | 最终工作树未发现空白错误 |
| 版本字段一致性 | PASS | 根 package、lockfile 顶层、lockfile 根包和插件 manifest 均为 `0.4.0`；README 固定安装示例与更新日志同步 |
| 生成运行时一致性 | PASS | 临时 TypeScript 编译产物与 `hooks/inject-context.js` 的 SHA-256 均为 `d384dfd3645cab356e6cf58e6ae48d821de5d748e63618ee41377e264a55f7a3` |
| 候选插件包内容 | PASS | 临时归档包含 manifest、三个 Hook 配置与运行时、核心 Skill、五个 reference、元数据和图标；未发现缓存、日志或 `.DS_Store` 噪音 |
| 隔离插件目录 Hook | PASS | 从隔离候选副本直接运行 `SessionStart`，输出 `STONEFISH ENGINEERING ACTIVE — v0.4.0` 并正确解析 reference 基准路径 |

## 行为与客户端验证

| 检查 | 结果 | 证据边界 |
| --- | --- | --- |
| 固定十案例重放 | PENDING | 发布门槛要求保存完整环境、原始输出、基线/候选多次运行和盲评；本地快速观察不能替代 |
| startup、逐轮、子 Agent | PENDING | 需要安装候选并在全新 Codex 任务验证注入版本及 reference 基准路径 |
| resume、clear、compact | PENDING | 需要真实客户端分别触发并记录，不能由配置或单元测试替代 |

## 发布决定

2026-08-31，在明确告知固定行为回归与真实客户端生命周期仍为 `PENDING`、默认安装跟踪 `main` 后，维护者确认发布 `v0.4.0`。本次决定接受这些未验证边界，但不把它们视为通过；候选提交后的真实归档验证与候选 CI 仍是打标签和创建 GitHub Release 前的硬门槛。

## 结论

`0.4.0` 的版本、文档和确定性候选门禁已经建立并通过。固定行为回归和真实客户端生命周期仍为 `PENDING`；维护者已在知情后接受这些边界并确认发布。提交、推送 `main` 可以继续，但打标签和创建 GitHub Release 仍须等待真实候选归档验证与候选 CI 通过；任何发布说明都不得把未完成项表述为已验证。
