# v0.5.0 候选验证

状态：准备版本提交，用于远端下载安装后验证；尚未创建 tag 或 GitHub Release，不作正式发布通过声明。

## 范围

- 核心 Skill 与五份 references 独立分发，Hook 插件不再包含或注册 Skill。
- 三个生命周期 Hook 只发送加载要求；工程决定前完整读取核心，细则按需读取。
- 根包、锁文件与插件 manifest 的候选版本统一为 `0.5.0`；不升级依赖。
- 完整开发过程及既有冒烟边界见[开发态验证](independent-skill-development.md)，不把旧版本结果当成本候选结果。

## 提交前检查

2026-09-07，使用 Node.js `24.16.0` 与 npm `11.13.0` 完成：

- `npm run check`：类型检查、构建、仓库校验及 7 项 Hook 测试通过。
- `npm run validate:release`：在候选文件进入暂存区后通过，只证明发布结构与文件跟踪，不代表行为验收或正式发布通过。
- 官方 Skill、插件校验器及变更文档的标点检查通过。
- 候选归档中 Hook 插件 6 个文件、独立 Skill 7 个文件，没有插件内 Skill 副本、日志或缓存噪音；归档 Hook 的三个事件和归档内 7 项测试通过。
- 在干净归档目录运行 `npm ci`、类型检查和构建，通过；重新生成的 Hook 与待提交版本逐字节一致。
- 使用 Skills CLI `1.5.24` 从候选归档安装到临时项目，通过；安装内容与候选 Skill 逐字节一致，Codex 原生目录和 Grok inspect 均只发现一份该 Skill。
- 安全、架构与四个对抗性角度复查完成；上游按名更新可能重新选择宿主的问题已通过 README 的显式目标更新命令说明，不新增更新器。

这些是本地候选证据；GitHub Actions、远端下载和真实全局安装仍待下一阶段记录。

## 下载与安装验收

先从包含候选提交的 GitHub 来源安装独立 Skill，再升级同一 marketplace 的 Hook 插件；不改成本地开发源。

待验证：远端下载内容与候选提交一致、插件版本为 `0.5.0`、只有一份原生 Skill 注册、旧全文 Hook 不再启用、实际缓存中三个 Hook 均返回加载要求。

## 重启与行为边界

新任务需重新核对 Hook 信任、完整核心读取与细则路径。startup、逐轮、子 Agent、resume、clear、compact 的真实插件流程和固定行为案例仍需分别记录；结构校验或 Hook 进程成功不替代这些证据。桌面调用样式与使用计数也不能由读取轨迹推断。
