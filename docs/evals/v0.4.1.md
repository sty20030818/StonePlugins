# v0.4.1 发布评测

本文件记录 `stonefish-engineering` v0.4.1 的候选证据。本次补丁只调整公开决策说明的 Markdown 层级，不改变核心规则、方法选择、Hook 配置或生命周期。

## 范围

- 发布基线：`v0.4.0`（`5960452`）。
- 一至两个决定：总标题和决定标题移到引用外；“影响与取舍”在引用内单独成段并加粗。
- 三个以上决定：继续使用紧凑表格。
- 用户已在 Codex 桌面渲染中确认最终方向；真实 `v0.4.1` 安装态仍需发布、更新并重启后验收。

## 确定性检查

| 检查 | 结果 | 证据边界 |
| --- | --- | --- |
| 仓库根级检查 | PASS | 类型检查、构建、仓库契约和 5 个 Hook 测试通过 |
| 发布态 Git 跟踪门禁 | PASS | 隔离候选副本中的全部发布文件已跟踪，`validate:release` 通过 |
| Skill 与插件校验 | PASS | 专用校验器通过；只证明结构，不替代客户端渲染 |
| 候选归档 | PASS | 插件目录包含预期的 13 个文件，没有缓存或日志噪音 |
| 隔离候选 Hook | PASS | 三个事件直接运行通过，核心注入使用 `v0.4.1`，逐轮提醒不回显用户输入 |

## 行为边界

固定行为案例不在本补丁中重放，因为决定语义、方法路由和 Hook 输出协议均未变化。此边界不等同于行为回归通过；本次只以模板门禁和用户确认覆盖 Markdown 呈现风险。

## 发布决定

2026-08-31，维护者确认发布 `v0.4.1`，并接受发布前无法从当前任务证明重启后真实客户端渲染的边界。打标签和创建 GitHub Release 仍须等待候选提交后的 GitHub Actions 通过。
